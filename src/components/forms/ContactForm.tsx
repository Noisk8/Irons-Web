'use client'; import { useState } from 'react'; import { useForm } from 'react-hook-form';
type Data={name:string;email:string;message:string}; export function ContactForm(){const [sent,setSent]=useState(false);const {register,handleSubmit,formState:{errors}}=useForm<Data>();if(sent)return <div className="rounded-xl bg-green-50 p-6">Mensaje recibido. Te responderemos pronto.</div>;return <form onSubmit={handleSubmit(()=>setSent(true))} className="space-y-5"><label className="block text-sm">Nombre<input {...register('name',{required:true})} className="mt-1 h-11 w-full rounded-md border px-3"/>{errors.name&&<span className="text-red-600">Campo requerido</span>}</label><label className="block text-sm">Correo<input type="email" {...register('email',{required:true})} className="mt-1 h-11 w-full rounded-md border px-3"/></label><label className="block text-sm">Mensaje<textarea {...register('message',{required:true,minLength:10})} className="mt-1 min-h-36 w-full rounded-md border p-3"/></label><button className="rounded-md bg-irons-charcoal px-6 py-3 text-white">Enviar mensaje</button></form>}
